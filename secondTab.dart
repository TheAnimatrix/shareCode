
import 'package:flutter/material.dart';

class PublicCountdownPage extends StatelessWidget {

  PublicCountdownPage({key}):super(key:key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child:Text("Welcome to the Public countdown list")
      ),
    );
  }
}
